{{#> Wrapper }}
    {{#> Box }}
        {{> Input type="text" name="login" label="Логин:"}}
        {{> Input type="password" name="password" label="Пароль:"}}
        <div class="form_field">
            {{> Button id="button_registration" text="Вход"}}
        </div>
        <div class="form_field_link">
            <a href="#" class="form_field_link_auth">Регистрация</a>
        </div>
    {{/ Box }}
{{/ Wrapper }}
