.chat_create_button {
    color: var(--white_color);
    background: var(--main_color);
    font-size: 12px;
    border: none;
    outline: none;
    padding: 4px 8px;
    border-radius: var(--radius);
    margin-top: 10px;
    cursor: pointer;
}
