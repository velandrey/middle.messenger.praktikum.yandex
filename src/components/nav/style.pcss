.nav {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
}
.nav__link {
    color: var(--highlighted-color);
    text-decoration: none;
    display: block;
    padding: 10px 10px;
}
.nav__link.active {
    color: var(--main_color);
}